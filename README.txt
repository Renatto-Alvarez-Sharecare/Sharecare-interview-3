Problem: Car Rental System

Given a faulty car rental system:
 1. find the faulty tests and correct them
 2. correct the faulty code causing the system to not work correctly.
 3. using TDD -- create a rental list handler
 4. using TDD -- create a command that will run over rentals that are past due for the return,
    will send an email (mock) to admins and the customers that are past due.
